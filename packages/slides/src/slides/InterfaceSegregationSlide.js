export const InterfaceSegregationSlide = () => (
  <section>
    <section>
      <h2 className="principle">Interface segregation principle</h2>
      <em>Keep interfaces small so that users don't end up depending on things they don't need.</em>
    </section>
    <section data-markdown="markdown/isp-example.md"></section>
  </section>
);
